{
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "cd ../.. && npm -w packages/engine run build && cd apps/web && next build",
  "functions": {
    "src/app/api/audit/route.ts": {
      "maxDuration": 60,
      "memory": 512,
      "regions": ["iad1"]
    }
  }
}
